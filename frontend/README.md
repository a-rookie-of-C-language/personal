# Personal Frontend

个人主页前端，使用 React + TypeScript + Vite。当前按纯静态站点部署，不依赖后端服务。

## 开发

```powershell
npm install
npm run dev
```

## 构建

```powershell
npm run build
```

部署 `dist` 目录即可。

## 内容

主要内容在 `src/siteContent.ts` 中维护。
